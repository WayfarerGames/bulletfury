# Samples

This folder contains Unity Package Manager samples for `com.wayfarergames.bulletfury`.

Samples should be organized into subfolders and referenced in `package.json` under the `samples` array.
